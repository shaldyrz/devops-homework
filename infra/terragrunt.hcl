# Root Terragrunt configuration

# This root configuration can define global variables, provider generation, 
# or backend configuration (e.g. local backend config for state storage).
# For local developments, we run terraform state locally inside each module folder.
