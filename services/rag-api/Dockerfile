FROM python:3.12-slim

WORKDIR /app

COPY services/rag-api/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY services/rag-api/app.py .
COPY corpus ./corpus

EXPOSE 8080

CMD ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8080"]
